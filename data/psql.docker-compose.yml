services:
  db:
    image: "postgres:16.13"
    container_name: "postgres"
    restart: unless-stopped
    ports:
      - 5437:5432
    volumes:
      - draftbot_db:/var/lib/postgresql/data
    env_file:
      - ../.env

volumes:
  draftbot_db:
    external: true